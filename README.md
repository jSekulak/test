git init
git status
git add ./README.md
git rm --cached .\README.md
git commit -m "Added readme file"
git log
git branch
git branch <myBranch>
git checkout <myBranch>
git remote add origin <link>
git branch -M main
git push -u origin main
git push -f origin master (if the previous command doesn't work)